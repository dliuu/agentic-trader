services:
  scanner:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file: ../.env
    volumes:
      - ../data:/app/data
      - ../config:/app/config
    restart: unless-stopped
